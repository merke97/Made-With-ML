{
  "name": "dr-archive-temporal-explorer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A temporal map of the DR archive — pan and zoom through broadcast history.",
  "scripts": {
    "dev": "vite",
    "server": "node server/index.mjs",
    "mock-kb": "node server/mock-kb.mjs",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pixi.js": "^8.6.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
